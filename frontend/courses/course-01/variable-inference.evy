// Often we write a declaration
// followed by an assignment:
s1:string
s1 = "Bla, bla, bla"

// We have a short form for this.
// We call it declaration with
// "type inference":
s2 := "💥"

// What does this program print❓
// Note, `print` can print one or
// more strings.
print s1 s2
